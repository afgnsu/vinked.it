.row
  .small-8.small-centered.columns
    = form_for(resource, as: resource_name, url: session_path(resource_name), class: "form-signin") do |f|
      %h1.form-signin-heading= I18n.t(".devise.sessions.signin")
      = f.text_field :email, class: "input-block-level", placeholder: I18n.t('.users.form.email'), autofocus: true
      = f.password_field :password, class: "input-block-level", placeholder: I18n.t('.users.form.password')
      = f.submit(class: 'btn btn-large', value: I18n.t(".devise.sessions.signin"))
      = render "devise/shared/links"
